#!/bin/bash

# Check if the script is run with an argument (username)
if [ -z "$1" ]; then
  echo "Usage: $0 <username>"
  exit 1
fi

# Run the 'whoami' command under the specified user
su - "$1" -c "whoami"
